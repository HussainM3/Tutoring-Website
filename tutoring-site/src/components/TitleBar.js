/* JS file for titlebar */

import React from "react";
import "./TitleBar.css";

function TitleBar(){
    return(
        <div className="title-bar">
            <h1>Math Tutoring Website</h1>
        </div>
    );
}

export default TitleBar;